{% extends "layout.html.twig" %}
{% block contenu %}
    <div class="row">
        {% if data is not empty %}
            <legend>Commandes</legend>
            <table>
                <caption>Recapitulatifs des commandes</caption>
                <thead>
                <tr><th>id commande</th><th>user_id</th><th>prix</th><th>date_achat</th><th>etat_id</th><th> - </th></tr>
                </thead>
                <tbody>
                {% for commande in data if data is not empty %}
                    <tr>
                        <td>{{commande.id}}</td><td>{{commande.user_id}}</td><td>{{ commande.prix }}</td><td>{{commande.date_achat}}</td><td>{{ commande.libelle }}</td><td><a href="{{ url('commande.showDetails', {id : commande.id }) }}"> Détails </a></td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            <a href="{{ url('manga.show') }}"><input type="submit" name="retourManga" value="Retour sur la page de gestion des mangas" /></a>
        {% else %}
            <p>Pas de commandes <a href="{{ url('manga.index') }}"><input type="submit" name="retourManga" value="Retour sur la page de gestion des mangas" /></a></p>
        {% endif %}
    </div>
{% endblock %}